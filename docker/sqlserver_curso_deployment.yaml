apiVersion: apps/v1
kind: Deployment
metadata:
  name: sqlserver-curso-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sqlserver-curso
  template:
    metadata:
      labels:
        app: sqlserver-curso
    spec:
      containers:
        - name: sqlserver-curso
          image: vilianeso/academyio.sqlserver.cursos:latest
          ports:
            - containerPort: 1433   # porta padrão do SQL Server
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: SA_PASSWORD
              value: "MeuDB@123"
