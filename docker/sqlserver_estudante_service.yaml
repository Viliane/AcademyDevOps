apiVersion: v1
kind: Service
metadata:
  name: sqlserver-estudante-service
spec:
  selector:
    app: sqlserver-estudante
  ports:
    - name: sqlserver
      port: 1433
      targetPort: 1433
  type: ClusterIP
