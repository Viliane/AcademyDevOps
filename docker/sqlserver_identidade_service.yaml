apiVersion: v1
kind: Service
metadata:
  name: sqlserver-identidade-service
spec:
  selector:
    app: sqlserver-identidade
  ports:
    - name: sqlserver
      port: 1433
      targetPort: 1433
  type: ClusterIP
